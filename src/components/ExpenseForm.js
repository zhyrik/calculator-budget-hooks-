import React from 'react'

/**
 * functional react component for ...
 * @function
 * @param {*} props - props
 * @returns {JSX.Element} - react component
 * @useIn - ./
 */
function Form() {
  return (
    <div>
      expense form
    </div>
  )
}

export default Form